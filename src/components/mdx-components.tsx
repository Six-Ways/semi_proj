import { MathFormula, CrystalStructure, FermiDistribution } from '@/components/interactive';

// MDX组件映射，用于在MDX内容中使用自定义组件
export const mdxComponents = {
  MathFormula,
  CrystalStructure,
  FermiDistribution,
  // 可以在这里添加更多自定义组件
};